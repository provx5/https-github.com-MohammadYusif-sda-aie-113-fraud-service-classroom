[pytest]
markers =
    unit: fast unit-level tests for policy and feature extraction
    integration: end-to-end validation of domain/service contracts
    smoke: smoke tests for package entrypoints and batch execution

testpaths = tests
